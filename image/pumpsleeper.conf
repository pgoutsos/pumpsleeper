# =============================================================================
#  PumpSleeper — First Boot Configuration
#  Edit this file before inserting the SD card into your Raspberry Pi.
#  This file lives on the boot partition, which is readable on any Mac or PC.
#  Use a plain text editor (Notepad++, VS Code, TextEdit, nano).
# =============================================================================

# ── Home Network WiFi ─────────────────────────────────────────────────────────
# The Pi needs internet access on first boot to download PumpSleeper.
# Enter your home WiFi credentials here.
HOME_WIFI_SSID=
HOME_WIFI_PASS=

# ── Login credentials ─────────────────────────────────────────────────────────
# Default username is "pumpsleeper". Change the password below before booting.
SSH_PASS=pumpspy

# ── PumpSpy Hotspot ───────────────────────────────────────────────────────────
# The PumpSpy device will connect to this hotspot.
HOTSPOT_SSID=PumpSpyLab
HOTSPOT_PASS=pumpspy123
HOTSPOT_IP=192.168.50.1

# ── Home Assistant MQTT (optional) ────────────────────────────────────────────
# Leave MQTT_HOST blank to disable MQTT / Home Assistant integration.
MQTT_HOST=
MQTT_PORT=1883
MQTT_USER=
MQTT_PASS=
